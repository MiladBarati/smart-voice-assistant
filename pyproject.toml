[project]
name = "pjsua-installation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "elasticsearch>=7.0.0,<8.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.25.0",
    "urllib3>=1.26.0",
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "torch>=2.1.0; platform_system != 'Windows' or platform_machine != 'x86'",  # may require manual install on Windows
    "torchaudio>=2.1.0; platform_system != 'Windows' or platform_machine != 'x86'",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
